# Glow landing page components

This folder contains components and a dedicated page for the Glow APK landing page.

- `app/glow/page.tsx` — Glow landing page (route: `/glow`)
- `components/glow/*` — header, hero, features, screenshots, footer

Place the APK at `public/downloads/glow.apk` and visit `/glow` to preview.
